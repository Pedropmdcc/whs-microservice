server:
  port: 8082
spring:
  application:
    name: order-client
  data:
    mongodb:
      database: test
      port: 27017
      host: localhost
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
