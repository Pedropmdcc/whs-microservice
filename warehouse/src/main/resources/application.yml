server:
  port: 8081
spring:
  application:
    name: warehouse-client
  data:
    mongodb:
      database: test
      port: 27017
      host: localhost
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true